#!/bin/bash

export BASE_DOMAIN="{{ promswarm_monitoring_base_domain }}"
export PROMETHEUS_USERS=$(bash prometheus_users.sh)
export ALERTMANAGER_USERS=$(bash alertmanager_users.sh)
export PROM2TEAMS_CONNECTOR="{{ promswarm_prometheus_teams_webhook_url }}"